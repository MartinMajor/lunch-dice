import React from "react";

export const DinnerValuesCtx = React.createContext({ values: {}, setValues: () => null });
export const DinnerPricesCtx = React.createContext({ values: {}, setValues: () => null });